class Record {
    constructor(
        title,
        skillEarned,
        startDate,
        endDate,
        comments
    ) {
        this.title = title,
        this.skillEarned = skillEarned,
        this.startDate = startDate,
        this.endDate =  endDate,
        this.comment = comments
    }
}